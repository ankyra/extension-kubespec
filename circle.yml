dependencies:
  override:
  - curl https://storage.googleapis.com/escape-releases-eu/escape-v0.19.0.tgz > /home/ubuntu/bin/escape.tgz
  - tar -C /home/ubuntu/bin -xvzf /home/ubuntu/bin/escape.tgz

compile:
  override:
  - escape release --skip-deploy

test:
  override:
  - echo "Already tested"

